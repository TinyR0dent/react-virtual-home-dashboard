const lightAliases = ['light', 'lamp', 'chandelier', 'sconce'];

const doorAliases = ['door', 'gate', 'entrance', 'exit'];

const presenceAliases = ['floor', 'room', 'area', 'zone'];

export { lightAliases, doorAliases, presenceAliases };
