// Safe tracked stub. Local generated types are stored in supported-types.local.d.ts

import '@hakit/core';

declare module '@hakit/core' {
  export interface CustomEntityNameContainer {
    names: `${string}.${string}`;
  }
}

export {};
